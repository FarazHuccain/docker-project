version: "3.8"

services:
  app:
    container_name: myapp-production
    build:
      context: .
      dockerfile: Dockerfile.production
    ports:
      - "8080:8080"
    env_file:
      - .env.production
    environment:
      - NODE_ENV=production
