services:
  app:
    build:
      context: .
      dockerfile: Dockerfile.production
    ports:
      - "80:3000"
    environment:
      - MONGO_URI=${MONGO_URI}
      - PORT=3000
    env_file:
      - .env
