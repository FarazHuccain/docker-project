version: "3.8"

services:
  app:
    container_name: myapp-staging
    build:
      context: .
      dockerfile: Dockerfile.staging
    ports:
      - "3000:3000"
    env_file:
      - .env.staging
    environment:
      - NODE_ENV=staging
